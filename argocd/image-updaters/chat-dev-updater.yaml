apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: chat-dev-updater
  namespace: argocd
spec:
  namespace: argocd
  applicationRefs:
    - namePattern: "chat-dev"
      images:
        - alias: "backend"
          imageName: "yuvrajsoam/chatapp-backend"
          commonUpdateSettings:
            updateStrategy: "semver"
          kustomizeSettings:
            kustomizationPath: "k8s/overlays/dev"
            newTagFieldName: "newTag"
        - alias: "frontend"
          imageName: "yuvrajsoam/chatapp-frontend"
          commonUpdateSettings:
            updateStrategy: "semver"
          kustomizeSettings:
            kustomizationPath: "k8s/overlays/dev"
            newTagFieldName: "newTag"
      git:
        writeBackMethod: "git"
        branch: "main"
        credentials:
          secretRef:
            name: argocd-image-updater-git-creds
            namespace: argocd
