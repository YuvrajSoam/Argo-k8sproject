apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  selector:
    matchLabels:
      app: frontend
  template:
    spec:
      containers:
      - name: chat-app-frontend
        env:
        - name: NODE_ENV
          value: development
