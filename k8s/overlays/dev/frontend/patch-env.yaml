apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  template:
    spec:
      containers:
      - name: chatapp-frontend
        env:
        - name: NODE_ENV
          value: development
