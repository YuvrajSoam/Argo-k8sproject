apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base/frontend

images:
- name: frontend
  newName: yuvrajsoam/chatapp-frontend
  newTag: v1.0.3 # or latest, or semver, or stable

patchesStrategicMerge:
- patch-env.yaml
- patch-replicas.yaml
