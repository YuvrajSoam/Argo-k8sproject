apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chatapp-ingress
  labels:
    name: chatapp-ingress
  annotations:
    # ngnix.ingress.kubernetes.io/readwrite-target: /
    # ngnix.ingress.kubernetes.io/ssl-redirect: "false"

    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - host: chattws.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 5001
